---
description: List all your glyphling pets (read-only).
---

Run `!glyphling pets` and print the resulting output verbatim.

Do not interpret, summarise, or expand on the output — the CLI already prints
a list of all pets with their level, mood, and active marker.
If the command exits non-zero, surface its stderr unchanged.
