---
description: Show your glyphling pet's current status (read-only).
---

Run `!glyphling status` and print the resulting output verbatim.

Do not interpret, summarise, or expand on the output — the CLI already prints
a compact multi-line summary with name, level, XP, mood, and age.
If the command exits non-zero, surface its stderr unchanged.
